# Spotify Clone Frontend

React + Tailwind CSS app with a global Light/Dark theme toggle.

## Theme behavior

- Tailwind `darkMode: "class"`
- Theme stored in `localStorage` under key `theme`
- Defaults to system preference (`prefers-color-scheme`) when no stored preference exists

## Scripts

- `npm install`
- `npm start`
- `npm run build`
